import styled from 'styled-components';

const CloseButton = styled.div`
  position: absolute;
  margin-top: 0.3em;
  right: 0;
  font-size: 1.5rem;
  cursor: pointer;
`;

export default CloseButton;
