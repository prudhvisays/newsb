import styled from 'styled-components';
import Select, { Option } from 'rc-select';

const FranchiseSelectStyle = styled(Select)`
  .rc-select-selection {
    color: #fff !important; 
    background: #333 !important;
  }
`;

export default FranchiseSelectStyle;
